# CONTEXT

## 이 디렉토리의 목적
AI 시대 개발자 채용 트렌드(디버깅 과제, 코드 리뷰 면접)에 대비해 **실전 연습**을 하는 공간.
개념 정리(이론, 용어, 패턴 등)는 **Obsidian 볼트**에서 관리한다 — 노트 간 유기적 연결(백링크)이
필요한 내용은 전부 그쪽으로. 이 디렉토리는 실습 결과물, 로그, 코드 스니펫 등 "실행 가능한 것"만 둔다.

## 배경 (왜 이걸 하는가)
`the-first-context-with-claude.md`에 있는 Gemini와의 대화에서 나온 문제의식:

- AI가 코드를 즉시 생성할 수 있게 되면서, 기존 LeetCode식 알고리즘 코딩테스트의 변별력이 사라짐.
- 그래서 기업들은 평가 축을 옮기는 중:
  - **올리브영**: 라이브 코딩 폐지 → "AI 과제 전형" (문제 정의, 프롬프트, 검증·개선 과정을 평가)
  - **Meta**: 2025.10~ 코딩 인터뷰를 3단계로 재편
    1. **버그 수정** — 낯선 코드베이스, AI 사용 금지인 경우 많음. off-by-one, 타입 캐스팅, 잘못된 조건문 등 "정교하지만 사소한" 버그 위주.
    2. **핵심 구현** — AI 사용 허용/기대. 평가 기준은 프롬프트 실력이 아니라 "AI가 짠 코드를 본인이 이해하고 있는가".
    3. **최적화** — 더 큰 입력으로 기존 솔루션을 깨서 최적화 필요성을 판단하는지 확인.
  - 공통 평가 기준: 문제 해결력 / 코드 품질 / 검증(실행해서 확인하는가) / 커뮤니케이션(사고 과정을 말로 설명하는가)
  - "AI를 크러치(crutch)로 쓰면 감점" — 결국 **코드를 읽고 판단하는 능력**이 핵심.

즉 목표는 "AI가 짠 코드를 검증·비판할 수 있는 눈"을 기르는 것. 알고리즘 암기가 아니라
**낯선 코드 읽기 + 디버깅 + 코드 리뷰 + 설계 판단**에 집중한다.

## 연습 방향 (정석 루틴)

### 디버깅
- 처음 보는 코드 + 숨겨진 버그를 시간 제한(15~20분) 걸고 찾는 연습.
- 순서: 재현 → 가설 → 검증. 감으로 고치지 않는다.
- 사고 과정을 소리 내어(또는 글로) 설명하는 연습을 항상 병행.
- 소스: HackerRank Debugging 섹션, hellointerview.com/practice

### 코드 리뷰
- AI(Claude/GPT)가 짠 코드를 표준 체크리스트로 리뷰:
  1. 정확성 (로직 오류, 엣지 케이스)
  2. 가독성/네이밍
  3. 중복/설계 (책임 분리)
  4. 성능
  5. 보안 (입력 검증, 인젝션)
  6. 테스트 커버리지
- 코멘트는 "왜 문제인지 + 어떻게 고칠지"까지 구체적 문장으로 작성.
- 소스: Google Engineering Practices (google.github.io/eng-practices), 오픈소스 PR 모의 리뷰

### 공통 습관
- 코드를 받으면 바로 손대지 말고 먼저 5분 읽기.
- AI 결과물은 그대로 붙여넣지 말고 실행해서 검증.
- "왜 이렇게 짰는지"를 항상 스스로에게 되묻기 (설명 못 하면 이해 못 한 것).

## 참고 자료
- hellointerview.com/learn/ai-coding — 이론(Fundamentals 5개 아티클) + 실전 문제
- HackerRank Debugging 섹션
- Google Engineering Practices (google.github.io/eng-practices)
- "The Software Engineer's Guidebook" (Gergely Orosz)

## 이 디렉토리 구조
- `CONTEXT.md` — 이 파일. 배경과 방향성.
- `vault/` — Obsidian 볼트. 개념/이론과 연습 코드를 전부 여기서 백링크로 연결한다.
  - `MOC.md` — 진입점(Map of Content).
  - `Concepts/` — 원자적 개념 노트 (노트 하나 = 개념 하나), `[[ ]]`로 상호 링크.
  - `Practice/` — 문제/세션별 폴더. 코드 + `note.md`(관련 개념 백링크 + 회고) 짝. 규칙은 `Practice/README.md` 참고.
