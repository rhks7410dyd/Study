디버깅 과제와 코드 리뷰 과제를 연습할 수 있는 방법이 뭐가 있을까?
디버깅과 코드 리뷰는 서로 다른 근육을 쓰는 활동이라 연습 방법도 좀 다르게 접근하는 게 좋아요. 하나씩 정리해볼게요.

## 디버깅 연습 방법

**1. 일부러 버그가 있는 코드로 연습하기**
- LeetCode나 백준 문제를 풀 때, 남이 짠 오답 코드(제출 기록에서 WA 받은 코드 등)를 가져와서 왜 틀렸는지 스스로 찾아보는 연습이 효과적이에요.
- GitHub에서 "fix bug" 커밋을 검색해서, 수정 전 코드만 보고 버그를 먼저 찾아본 다음 실제 수정 커밋과 비교해보는 것도 좋은 훈련입니다.

**2. 디버거 도구에 익숙해지기**
- `print`문으로만 디버깅하지 말고, VSCode나 IntelliJ의 브레이크포인트, watch 변수, call stack 확인 기능을 적극적으로 써보세요. 학부 과제에서부터 습관을 들이면 나중에 큰 프로젝트에서 시간이 크게 절약돼요.
- 파이썬이면 `pdb`, C/C++이면 `gdb` 명령줄 디버거도 한 번씩 다뤄보면 IDE 없는 환경(서버 등)에서도 대응이 됩니다.

**3. 재현 → 가설 → 검증의 루틴화**
- 버그를 만나면 바로 코드를 고치려 하지 말고: (1) 최소 재현 코드 만들기 → (2) "여기가 문제일 것 같다"는 가설 세우기 → (3) 실제로 그 부분만 찍어보거나 격리해서 검증하기, 이 순서를 의식적으로 연습하면 감으로 고치는 습관에서 벗어날 수 있어요.

**4. 의도적으로 버그 심어서 짝과 교환하기**
- 스터디나 팀 프로젝트에서 서로 코드에 버그를 하나씩 심고 교환해서 찾아보는 게임 형식으로 하면 재미도 있고 실력도 늘어요.

## 코드 리뷰 연습 방법

**1. 오픈소스 PR 리뷰 흉내내기**
- 관심 있는 오픈소스 저장소(너무 크지 않은 것)의 Pull Request 목록에서 아직 머지 안 된 PR을 골라, 실제 리뷰어의 코멘트를 보기 전에 스스로 리뷰 코멘트를 먼저 작성해보고 나중에 비교하는 방식이 좋아요.

**2. 체크리스트 기반으로 보기**
- 처음엔 "이 코드 좋아 보이나요?" 식으로 막연하게 보지 말고, 체크리스트를 만들어서 하나씩 확인하는 습관을 들이세요. 예를 들면:
  - 네이밍이 의도를 잘 드러내는가
  - 엣지 케이스(빈 입력, null, 경계값)를 처리했는가
  - 중복 로직은 없는가
  - 함수/클래스의 책임이 하나로 명확한가
  - 에러 처리가 적절한가

**3. 팀 프로젝트에서 실제로 리뷰어 역할 자원하기**
- 캡슐톤이나 팀플에서 PR 리뷰를 서로 돌아가며 하되, 승인만 하지 말고 "왜 이렇게 짰는지" 질문하는 코멘트를 최소 1~2개는 남기는 규칙을 정해보세요. 질문을 던지는 것 자체가 리뷰 실력을 키우는 좋은 훈련이에요.

**4. 유명 스타일 가이드 읽고 대조하기**
- Google Style Guide나 언어별 공식 스타일 가이드를 읽고, 자신이 최근에 짠 코드나 리뷰한 코드가 그 기준에 얼마나 부합하는지 대조해보는 것도 좋습니다.

---

혹시 지금 진행 중인 캡슐톤 프로젝트(온더블록)나 SeniorVibe 코드베이스에서 실전으로 적용해볼 만한 구체적인 상황이 있다면, 그 코드를 가지고 같이 리뷰 연습을 해볼 수도 있어요.

아니 그 기업들 면접준비용도라 정석적인 것들을하고싶음

기업 면접 대비라면 목적이 명확하니 좀 더 표준화된 루틴으로 잡아드릴게요.

## 디버깅 인터뷰 대비

**형식 이해하기**
기업 인터뷰에서 디버깅은 보통 두 가지 형태로 나와요.
1. **버그가 있는 코드를 주고 시간 내에 고치기** (Google, Amazon 등에서 종종 사용)
2. **알고리즘 문제를 풀다가 나온 자신의 버그를 스스로 찾아 고치기** (라이브 코딩 중 실시간으로 평가)

**연습 방법**
- **HackerRank의 "Debugging" 섹션**: 실제로 버그가 삽입된 코드를 주고 고치는 문제들이 카테고리로 따로 있어요. 인터뷰 형식과 가장 유사합니다.
- **LeetCode "Discuss" 탭 활용**: 문제 풀고 나서 Discuss 탭에서 "Wrong Answer" 케이스를 올린 사람들의 코드를 보고, 정답 보기 전에 먼저 원인을 찾아보는 연습을 반복하세요.
- **시간 제한 걸고 연습**: 실전에서는 보통 15~20분 안에 버그를 찾아야 하니, 평소에도 타이머를 켜고 연습하는 게 중요해요.
- **말로 설명하며 디버깅하기**: 인터뷰에서는 침묵 속에서 코드만 고치면 안 되고, "이 부분이 인덱스 오프바이원 에러일 가능성이 있어서 확인해보겠습니다" 식으로 사고 과정을 소리 내어 말하는 훈련이 필요해요. 혼자 연습할 때도 실제로 소리 내어 설명하면서 하는 게 효과가 큽니다.

## 코드 리뷰 인터뷰 대비

**형식 이해하기**
코드 리뷰 인터뷰는 주로 시니어 포지션이나 일부 스타트업(Shopify, GitLab 같은 원격 중심 회사)에서 쓰는데, 실제 PR diff나 코드 스니펫을 주고 리뷰 코멘트를 작성하게 합니다. 평가 기준은 보통:
- 버그/엣지 케이스를 잡아내는가
- 코멘트가 건설적이고 구체적인가 (그냥 "이상해요"가 아니라 "왜, 어떻게 고칠지"까지)
- 우선순위를 구분하는가 (블로커 vs 사소한 스타일 지적)

**연습 방법**
- **실제 오픈소스 PR로 모의 리뷰 작성**: 관심 있는 저장소에서 아직 안 머지된 PR을 골라, 실제 리뷰어 코멘트를 가리고 스스로 먼저 리뷰를 써본 뒤 비교하세요.
- **표준 체크리스트로 구조화**: 인터뷰에서는 즉흥적으로 보지 말고 아래 순서로 훑는 습관을 들이면 빠지는 부분이 줄어듭니다.
  1. 정확성 (로직 오류, 엣지 케이스)
  2. 가독성/네이밍
  3. 중복/설계 (책임 분리가 잘 되어 있는가)
  4. 성능
  5. 보안 (입력 검증, 인젝션 등)
  6. 테스트 커버리지
- **코멘트를 실제로 글로 써보기**: 말로만 생각하지 말고 "이 함수는 null 체크가 빠져서 NPE 위험이 있습니다. 3번째 인자에 대해 early return을 추가하는 건 어떨까요?"처럼 구체적 문장으로 작성하는 연습을 반복하세요. 인터뷰에서는 이 문장력 자체가 평가 대상이에요.

## 공통으로 참고할 만한 자료
- **"The Software Engineer's Guidebook" (Gergely Orosz)**: 코드 리뷰 문화와 시니어 레벨 평가 기준을 다루는 챕터가 있어서 표준을 잡기 좋아요.
- **Google Engineering Practices 문서 (google.github.io/eng-practices)**: 구글 내부 코드 리뷰 가이드라인이 공개되어 있는데, "무엇을 봐야 하는가"의 표준 기준으로 널리 인용돼요.

혹시 목표로 하는 특정 기업이 있으면, 그 회사가 실제로 어떤 방식의 인터뷰를 쓰는지 최근 후기를 찾아드릴 수도 있어요.

그게 아니라 이 내용을 물어보는이유가 아래 제미나이와 대화 때문임
(아래는 제미나이와의 대화 복사본)
AI의 도입 이후로 기존의 코딩 테스트는 거의 무의미해졌다고 보고 취업떄 해당 부분을 회사들이 어떻게 판단하는지에 대해 컬럼이나 정보를 최대한 긁어서 알려줘

AI의 발전으로 기존 코딩 테스트의 실효성에 대한 의문이 커지면서 기업들의 채용 방식도 크게 변화하고 있습니다. 특히 생성형 AI를 활용하여 정답을 쉽게 도출할 수 있게 됨에 따라 단순히 정해진 답을 코드로 구현하는 능력을 평가하는 것은 무의미해졌다는 평가가 지배적입니다.  
Karat

최근 기업들이 이러한 변화를 채용에 어떻게 반영하고 있는지, 취업 시장의 핵심 트렌드를 정리해 드립니다.  
HackerRank

1. 'AI 네이티브' 역량 검증으로의 전환  
Daum
단순히 AI 도구를 다룰 줄 아는 'AI 리터러시'를 넘어, AI와 협력하여 실질적인 문제를 해결하는 'AI 네이티브' 역량이 가장 중요한 평가 기준으로 부상했습니다.  
Daum

올리브영 사례: 최근 올리브영은 개발 직군 경력 채용에서 전통적인 라이브 코딩 테스트를 폐지하고 'AI 과제 전형'을 신설했습니다. 이는 AI를 활용해 문제를 정의하고, 프롬프트를 작성하며, 결과를 검증하고 개선하는 전 과정을 종합적으로 평가하기 위함입니다.  
Daum
+ 2

Meta(메타) 사례: 실제 업무 환경과 유사한 조건을 조성하기 위해 면접 과정에서 AI 도구 사용을 권장하는 실험을 진행하기도 했습니다.  
HackerRank
+ 1

이제는 "얼마나 코드를 잘 짜는가"가 아니라 "AI 도구를 활용해 문제를 얼마나 빠르고 정확하게 해결하는가"를 평가하는 방향으로 나아가고 있습니다.  
Daum

2. '결과'보다는 '과정'과 '사고력' 중심 평가
AI가 정답에 가까운 코드를 1초 만에 만들어낼 수 있기 때문에, 결과물 자체보다는 그 결과에 도달하기까지의 과정과 후보자의 사고력을 평가하는 데 집중하고 있습니다.

문제 해결 과정 추적: 과제 전형에서 단순한 결과물 제출을 넘어, 문제를 정의한 방식, AI에 입력한 프롬프트, 도출된 코드를 수정하고 개선한 과정 등 전반적인 아키텍처 설계 능력을 봅니다.  
DEV Community
+ 1

심층적인 후속 질문: 면접관은 후보자가 AI가 생성한 코드를 명확히 이해하고 있는지, 왜 해당 접근 방식을 선택했는지 묻습니다. 뛰어난 지원자는 AI의 제안을 비판적으로 수용하고 이유를 설명할 수 있지만, 그렇지 못한 지원자는 독립적인 추론이 필요한 추가 질문에 답하지 못합니다.  
Level Up Coding - GitConnected
+ 2

개인화되고 모호한 문제 출제: AI로 쉽게 풀 수 있는 획일화된 알고리즘 문제 대신, 특정 상황이 주어지거나 모호성이 포함된 문제를 제시하여 지원자의 실질적인 판단력을 평가합니다.  
karat.com
+ 1

3. 실무 환경 기반의 종합적 평가 도입
단순 코딩 테스트를 대체하기 위해 기업들은 지원자의 종합적인 실무 능력을 평가할 수 있는 다양한 방식을 도입하고 있습니다.  
HackerRank

해커톤 및 오픈소스 기여도 활용: 올리브영은 크래프톤과 함께 AI 해커톤 대회를 개최하여 실제 유통 현장의 문제를 해결하도록 하고, 우수자에게 서류 전형 통과 혜택을 제공합니다. 또한 GitHub 활동 내역이나 포트폴리오를 통해 실제 개발 프로젝트 경험을 심층 평가합니다.  
Level Up Coding - GitConnected
+ 2

페어 프로그래밍(Pair Programming): 면접관과 지원자가 함께 문제를 해결하는 과정을 통해 의사소통 능력, 협업 태도, 문제 해결 방식을 실시간으로 확인합니다.  
Level Up Coding - GitConnected

💡 개발자를 위한 취업 전략
기존처럼 플랫폼에서 알고리즘 문제(예: LeetCode)만 푸는 방식으로는 경쟁력을 갖추기 어렵습니다. 앞으로는 AI를 능숙한 '부사수'로 활용하여 생산성을 극대화하고, 시스템 아키텍처 설계, 비즈니스 로직에 대한 이해, 복잡한 문제 해결 능력과 같이 AI가 대체할 수 없는 고차원적인 역량을 키우는 것이 핵심입니다.  
Level Up Coding - GitConnected
+ 1

그럼 해당 능력에 대해 회의적인 시선이 많아졌겠네 기존에도 많았는데

맞습니다. 아주 정확한 지적이십니다.

기존에도 "제한된 시간 안에 알고리즘 문제의 정답을 맞히는 것"이 실제 개발자의 실무 능력을 대변하지 못한다는 비판은 꾸준히 존재했습니다. LeetCode 같은 플랫폼에서 특정 알고리즘 패턴을 암기해 기계적으로 문제를 푸는 능력이, 실제 서비스의 복잡한 비즈니스 로직을 이해하고 설계하는 역량과 직결되지는 않기 때문입니다.

그런데 생성형 AI의 등장이 이러한 회의론에 쐐기를 박은 셈이 되었습니다.

AI가 코딩 테스트 회의론을 가속화한 이유
구현 능력의 가치 하락: 예전에는 복잡한 시간 복잡도를 계산하고 최적화된 알고리즘을 코드로 구현하는 것 자체가 하나의 '기술'이었습니다. 하지만 이제는 LLM에게 문제 상황과 조건만 던져주면 순식간에 최적화된 코드를 짜줍니다. 정해진 입출력에 맞춰 코드를 생성하는 능력 자체의 변별력이 사라진 것입니다.

실무와의 괴리 심화: 실무 개발은 단순히 독립된 함수 하나를 만드는 것이 아니라, 거대한 시스템 안에서 기존 코드와 상호작용하며 이루어집니다. 코딩 테스트는 이러한 '맥락(Context)'을 평가할 수 없다는 한계가 있었는데, AI 시대에는 전체 아키텍처를 조망하는 능력이 훨씬 중요해지면서 그 한계가 더욱 부각되었습니다.

기업들의 평가 기준 변화: "코딩"에서 "엔지니어링"으로
결국 단순한 '코더(Coder)'는 AI로 대체할 수 있다는 인식이 퍼지면서, 기업들은 코딩 테스트를 최소한의 논리력을 보는 '통과 의례' 정도로만 남겨두고, 다음과 같은 고차원적인 엔지니어링 역량을 검증하는 데 사활을 걸고 있습니다.

시스템 아키텍처 설계 역량: 단순한 기능 구현을 넘어 FastAPI, Spring Boot, Go, Rust 등 다양한 환경의 특성을 이해하고, 확장성과 안정성을 고려한 백엔드 구조를 설계할 수 있는지 평가합니다.

신기술의 실무 적용 및 통합 능력: LLM을 단순히 써본 것을 넘어, RAG(검색 증강 생성) 파이프라인을 구축하거나 Function Calling을 활용해 실제 챗봇 서비스의 복잡한 시나리오와 예외 상황을 처리할 수 있는지를 봅니다.

복잡한 문제 해결과 디버깅: 외부 하드웨어(BLE 등) 연동 이슈나 비동기 통신에서 발생하는 예기치 못한 버그를 마주했을 때, 어떤 논리적 흐름으로 원인을 추적하고 해결하는지를 심층 면접으로 파고듭니다.

결국 취업 시장에서는 알고리즘 정답률보다, 스스로 부딪히며 만들어낸 서비스의 깊이와 기술적 의사결정 과정이 훨씬 강력한 무기가 된 상황입니다.

최근 채용 트렌드가 이렇게 실무 프로젝트나 아키텍처 설계 중심으로 뚜렷하게 변하고 있는데, 앞으로 취업 준비 과정에서 본인의 프로젝트 포트폴리오나 기술적 강점을 어떤 방향으로 어필해 볼 계획이신가요?

근데 실제 코드를 볼 수 있는 눈이 없으면 의미가없잖아그럴거면 기획자 100명 뽑지 개발자를 뽑진 않을거 아니야

정확한 핵심을 짚으셨습니다. 바로 그 지점 때문에 "알고리즘 코딩 테스트의 실효성은 떨어졌지만, 개발자의 근본적인 코딩 역량은 절대 죽지 않았다"고 업계 전문가들이 입을 모으는 것입니다.

말씀하신 대로 코드를 읽고 검증할 수 있는 '눈'이 없다면, AI가 만들어낸 코드는 겉보기에만 그럴싸한 시한폭탄과 같습니다. 기획자 100명이 AI를 활용해도 결코 개발자를 대체할 수 없는 결정적인 이유가 바로 여기에 있습니다.

1. 기획자 100명을 뽑지 않는 이유: '통합'과 '책임'
파편화된 코드의 통합(Integration): AI가 만들어주는 것은 결국 훌륭하게 짜인 '파편화된 코드 조각'입니다. 이 조각들이 복잡한 백엔드 프레임워크 내에서 메모리 누수 없이 톱니바퀴처럼 돌아가게 만들고, 데이터베이스 트랜잭션을 안전하게 관리하는 것은 기획자가 할 수 없는 영역입니다.

엣지 케이스와 하드웨어/네트워크 변수: AI는 전형적인 상황에 대한 코드는 기가 막히게 짜지만, 실제 비즈니스 로직에 얽힌 복잡한 예외 상황이나 외부 기기 연동, 비동기 통신에서 발생하는 미세한 타이밍 이슈 등을 스스로 예측하고 해결하지는 못합니다.

결과에 대한 책임: 상용 서비스에서 장애가 발생했을 때, "AI가 짜준 코드라서 원인을 모르겠습니다"라고 변명할 수는 없습니다. 서버가 멈췄을 때 로그를 까보고, 문제의 원인을 추적(Debugging)해 핫픽스를 배포하는 책임은 오롯이 코드를 깊이 이해하는 개발자의 몫입니다.

2. '작성자(Writer)'에서 '기술 검열관(Reviewer)'으로
과거의 개발자가 빈 화면에서 '0을 1로 만드는 타이핑 능력'이 중요했다면, AI 시대의 개발자는 AI가 쏟아낸 100개의 코드 중에서 진짜 완벽하게 작동하는 1을 골라내고 다듬는 '코드 리뷰' 능력이 핵심이 되었습니다.

진짜 실력 있는 개발자만이 AI가 짠 코드에서 다음과 같은 허점을 발견할 수 있습니다.

"이 방식은 트래픽이 몰리면 특정 데이터베이스 쿼리에 병목이 생기겠네."

"이 라이브러리를 쓰면 우리 회사의 기존 보안 정책과 충돌하겠는데?"

"비동기 처리가 제대로 안 되어서 메모리 릭(Leak)이 발생할 가능성이 있네."

3. 기업들은 그 '눈'을 어떻게 평가하는가?
기업들은 지원자가 '코드를 볼 줄 아는 눈'이 있는지 확인하기 위해 기존의 획일화된 알고리즘 테스트 대신 실무형 검증 방식을 도입하고 있습니다.

디버깅(Debugging) 과제: 처음부터 코드를 짜게 하는 대신, 실무와 유사한 거대한 코드베이스(교묘하게 버그가 숨어있는)를 던져주고 문제의 원인을 찾아 최적화하게 만듭니다.

코드 리뷰(Code Review) 면접: AI가 작성한 코드나 면접관이 준비한 특정 로직을 보여주고, "이 코드의 취약점이 무엇인지, 아키텍처 관점에서 어떻게 개선할 수 있을지"를 토론합니다.

결론적으로 단순 구현을 위한 '단순 코더'의 자리는 위협받고 있지만, 전체 시스템을 조망하고 코드를 비판적으로 읽어낼 수 있는 '진짜 소프트웨어 엔지니어'의 가치는 오히려 과거보다 훨씬 더 높아졌습니다.

최근 기업들이 진행하는 '코드 리뷰 면접'이나 '디버깅 테스트'의 실제 예시가 궁금하신가요?

아하, 그러면 이해가 되네요. 젬미나이와의 대화에서 나온 내용 — 코딩 테스트가 예전만큼 의미 없어지고, 대신 **디버깅 과제**랑 **코드 리뷰 면접**이 실제 채용 트렌드로 자리잡고 있다는 것 — 이게 배경이었던 거네요. 그래서 아까 "정석적인 걸 하고 싶다"고 하신 게, 이 트렌드에 맞춰서 면접 준비를 하고 싶으신 거였고요.

정리하면 젬미나이 대화의 핵심 논지는:
- 단순 알고리즘 문제 풀이(LeetCode식)는 AI 때문에 변별력을 잃음
- 대신 기업들은 **AI가 짠 코드를 검증하고 비판적으로 읽어낼 수 있는 능력**을 보려고 함
- 그 검증 수단으로 **디버깅 과제**(버그 있는 코드베이스에서 원인 추적)와 **코드 리뷰 면접**(AI 생성 코드나 로직을 보고 취약점 토론)이 실제로 쓰이고 있음

이 맥락이라면 제가 이전에 드린 답변 방향(HackerRank Debugging 섹션, 오픈소스 PR 모의 리뷰, Google Engineering Practices 등)이 실제로 이 트렌드에 맞는 정석적인 준비 방법이었던 거고요.

혹시 지금 원하시는 게 다음 중 어느 쪽인가요?

질문: 지금 어떤 방향으로 이어가고 싶으세요?
답변: 1,2번
두 가지 다 정리해서 드릴게요.

## 1. 실제 사례로 본 트렌드

**올리브영 (CJ올리브영)**
지난주 실제로 개발 직군 경력채용에서 기존의 라이브 코딩을 없애고 AI 과제를 신설했어요. AI를 이해하는 단계를 넘어 AI와 협업해 문제를 해결하는 AI 네이티브 인재를 선발하기 위함이라고 밝혔습니다. 구체적으로는 서류 심사 → AI 과제 → 1차 면접 → 온라인 인성검사 및 2차 면접 순으로 전형이 진행되고, 문제 정의 방식과 AI 활용 능력, 프롬프트 작성 과정, 반복 개선 및 결과 검증 방식 등 전체 프로세스를 구조화해 평가합니다. 즉 "정답 코드"가 아니라 "AI를 어떻게 다루고 검증했는가"의 과정 자체를 보는 거예요.

**Meta (더 흥미로운 사례)**
Meta는 2025년 10월부터 코딩 인터뷰 자체를 3단계 구조로 바꿨는데, 이게 지금 질문하신 "디버깅+코드리뷰가 정석"이라는 방향과 정확히 일치합니다.

- **Phase 1 (버그 수정)**: 처음 보는 코드베이스에 버그가 있고, 이걸 찾아서 고치는 단계. 흥미로운 건 이 단계에서 AI 사용 규칙이 면접관마다 다르다는 점 — 일부 면접관은 "이 단계는 AI 없이" 라고 명시하기도 합니다. 버그 유형은 대부분 알고리즘 문제가 아니라 타입 캐스팅 오류, off-by-one 에러, 잘못된 조건문 같은 것들이에요.
- **Phase 2 (핵심 구현)**: 여기서 AI 사용이 명시적으로 허용되고 기대됩니다. 다만 평가 기준은 프롬프트 엔지니어링이 아니라, 후보자가 AI가 생성한 코드를 실제로 이해하고 있는지예요.
- **Phase 3 (최적화)**: 더 큰 입력으로 앞선 솔루션을 깨뜨려서 최적화가 필요한지 판단하는 능력을 봅니다.

평가 기준 4가지가 인상적인데: 문제 해결력, 코드 품질, 검증(AI 결과물을 실행하고 확인하는가), 커뮤니케이션(사고 과정을 소리 내어 설명하는가)입니다. 그리고 이 부분이 핵심인데 — "AI를 크러치(crutch)로 쓰면 감점될 수 있다"고 리크루터가 명시적으로 경고했다고 해요. 결국 님이 아까 지적하신 "코드 볼 줄 아는 눈"이 없으면 AI를 써도 티가 난다는 게 실제로 평가 항목에 들어가 있는 셈이에요.

## 2. 정석 연습 방법 (위 사례 기반으로 재구성)

**디버깅 연습**
- Meta 방식처럼 "처음 보는 코드 + 숨겨진 버그"를 시간 제한 걸고 푸는 연습이 가장 실전에 가까워요. HackerRank의 Debugging 섹션, 혹은 Hello Interview의 Code Review/AI Coding 연습 섹션(hellointerview.com/practice)에 이런 유형의 문제가 공개돼 있습니다.
- 버그 유형이 대부분 off-by-one, 타입 캐스팅, 잘못된 조건문처럼 "정교하지만 사소한" 것들이라는 점을 기억하고, 알고리즘 자체보다 코드를 꼼꼼히 읽는 연습에 집중하세요.
- AI 없이 먼저 풀어보고, 그다음 AI(Claude/GPT)에게 같은 코드를 주고 어떻게 다르게 접근하는지 비교하는 것도 좋은 훈련이에요 — 실제 면접에서도 "AI 없이 먼저 해보고 안 되면 AI로 넘어가는" 순서가 좋은 평가를 받았습니다.

**코드 리뷰 연습**
- AI가 짠 코드를 review하는 연습이 핵심이에요. Claude나 ChatGPT에게 알고리즘 문제를 풀게 한 다음, 그 결과물에서 일부러 오류를 찾아내는 연습(엣지케이스 누락, 비효율적 자료구조, 잠재적 버그)을 반복하세요.
- 코드를 받으면 바로 "왜 이렇게 짰는지" 설명할 수 있어야 해요 — Meta 면접관들이 실제로 "이 코드가 왜 lock을 쓰고 semaphore를 안 썼는지 설명 못 하면, 그건 AI가 짠 거지 당신이 짠 게 아니다"라는 기준으로 평가한다고 합니다.
- 오픈소스 PR 리뷰 연습(전에 말씀드린 방법)도 여전히 유효하지만, 여기에 "AI가 생성한 코드"라는 조건을 하나 추가해서 연습하면 더 실전에 가까워요.

**공통으로 챙길 습관**
- 코드를 받으면 바로 손대지 말고 5분 정도 읽는 시간을 갖는 습관 — Meta 면접에서 이걸 건너뛴 후보자들이 공통적으로 후회했다고 해요.
- AI 결과물을 그대로 붙여넣지 말고, 실행해서 확인하고 소리 내어 설명하는 루틴을 몸에 익히는 것.

혹시 지금 준비하시는 회사가 정해져 있다면, 그 회사가 이런 AI 기반 전형을 쓰는지 구체적으로 찾아드릴 수도 있어요.

이거 관련된 사이트가 없어? 개인적으로 이런 부분에 대한 문제나 이론 정립을 한 사이트가 있을 것 같은데
네, 실제로 이 흐름에 맞춰서 이론과 문제를 정리해둔 사이트들이 몇 개 있어요. 하나씩 확인해서 정리해드릴게요.네, 정확히 딱 그런 사이트가 있어요. **Hello Interview (hellointerview.com)** 가 최근에 이 흐름에 맞춰서 아예 별도 커리큘럼을 만들어놨습니다.

## Hello Interview — "AI Coding" 섹션

기존에 시스템 디자인/DSA 강의로 유명한 사이트인데, 최근 이 트렌드에 맞춰 **learn/ai-coding** 이라는 전용 코스를 신설했어요. 구성이 꽤 체계적이에요.

**이론 파트 (learn/ai-coding)**
- `overview/introduction`: AI 기반 코딩 인터뷰의 두 가지 주요 포맷과 준비법, 회사들이 실제로 테스트하는 패턴들을 다루는 개요
- `overview/how-to-prepare`: 알고리즘은 암기가 아니라 인식/검증 목적으로 여전히 중요하고, 낯선 코드 읽기가 기존 준비 과정에서 가장 훈련이 안 된 스킬이라는 점, AI와 함께 시간 압박 속에서 코딩하는 연습이 가장 중요한 활동이라는 점을 강조합니다.
- `overview/patterns`: 그래프 탐색, 위상 정렬, 백트래킹 같은 패턴들을 다루는데, 패턴을 알면 모호한 문제를 정확한 프롬프트로 바꿀 수 있다는 게 핵심 논지예요.
- `overview/interview-formats`: 전형적인 포맷은 3단계 — 버그 수정 → 새 기능 구현 → 큰 입력에 대한 최적화 순서로 진행된다는 걸 미리 알아두면 시간 배분에 도움이 된다고 설명합니다.
- **Fundamentals (5개 아티클)**: codebase orientation(낯선 코드 파악법), planning your approach, driving the AI(AI 통제하기), verification and testing, communication — 이게 사실상 "AI 시대 코드 리뷰/디버깅 정석 이론"에 가장 가까워요.

**실전 연습 파트 (practice/ai-coding, practice/code-review)**
- 브라우저 안에서 코드베이스를 읽고, 버그를 고치고, 기능을 만들고, 테스트 스위트 대비 성능을 조이는 가이드형 연습 문제들이 있고, 자신의 에디터와 AI 툴로 로컬에서 풀고 제출한 뒤 디브리핑 질문에 답하면 피드백을 받는 방식도 지원합니다.
- 회사별 실제 문제 풀 (Meta, LinkedIn, Shopify)도 커뮤니티 섹션에 정리되어 있어요.

## 함께 참고할 만한 다른 자료
- **Canva Engineering Blog** — "Yes, You Can Use AI in Our Interviews"라는 포스트가 있는데, 회사 입장에서 왜 AI 사용을 허용/권장하는지, 좋은 사용 예시가 뭔지 실제 채용 담당자 시각에서 설명해요.
- **CoderPad — State of Tech Hiring 2026 리포트** — 여러 회사(구글, 리플링 등)의 AI 인터뷰 채택 현황을 데이터로 정리한 보고서예요.
- **interviewing.io 블로그** — Meta 등 특정 회사의 AI 인터뷰 실전 프롬프트 예시를 다루는 글들이 있어요.

정리하면, **hellointerview.com/learn/ai-coding**가 지금 원하시는 "이론 정립 + 문제 연습"이 한 곳에 모여있는 가장 정석적인 사이트예요. Fundamentals 5개 아티클부터 순서대로 읽고, practice 섹션에서 가이드형 문제를 풀어보는 순서로 접근하시면 될 것 같습니다.

Merge Intervals Variant. Not the classic "given a list of intervals, merge them." Instead, you're given addInterval() and insertInterval() method signatures and need to choose the right data structure for bookkeeping while handling overlapping intervals. Also reported at Staff level with a design-oriented framing.
이게 일반적으로 어떤 의미야

이건 정확히 **"자료구조 설계형(LLD, Low-Level Design) 인터벌 문제"**로 분류되는 유형이에요. 왜 "일반적인 Merge Intervals와 다른지", 그리고 "무엇을 평가하려는 건지" 나눠서 설명할게요.

## 기존 Merge Intervals (LeetCode 56)와 뭐가 다른가

**기존 문제**: `[[1,3],[2,6],[8,10],[15,18]]` 처럼 **한 번에 전체 리스트**가 주어지고, 이걸 정렬 → 한 번 순회하면서 겹치는 구간을 합치면 끝. 정적(static) 데이터에 대한 **일회성 배치(batch) 처리**예요.

**이 변형 문제**: `addInterval(start, end)`, `insertInterval(interval)` 같은 **메서드**가 주어져요. 즉 인터벌이 **하나씩, 시간차를 두고 계속 들어오는(online/streaming) 상황**을 가정하는 거예요. 예를 들면:

```
addInterval(1, 3)
addInterval(8, 10)
addInterval(2, 6)   // 기존 [1,3]과 겹침 → 병합 필요
insertInterval(4, 9) // 기존 [1,6], [8,10] 둘 다와 겹침 → 셋을 하나로 병합
```

매번 새 인터벌이 들어올 때마다 **"항상 겹치지 않는 상태"를 유지**해야 하고, 다음 조회를 위해 이 상태를 계속 들고 있어야 해요. 그래서 이건 알고리즘 문제라기보다 **"클래스를 어떻게 설계할 것인가"**의 문제가 됩니다.

## 왜 "자료구조 선택"이 핵심 평가 포인트인가

순진하게 접근하면:
```python
# 매번 배열 전체를 순회 → O(n)
def addInterval(self, start, end):
    self.intervals.append((start, end))
    self.intervals = merge_all(self.intervals)  # O(n log n) 매번!
```
이렇게 하면 매번 추가할 때마다 O(n log n)이 걸려요. 인터벌이 수천 개 쌓이면 매우 비효율적이죠.

그래서 면접관이 정말 보고 싶은 건 **"겹치는 이웃 구간을 빠르게 찾을 수 있는 자료구조"**를 선택하는 판단력이에요. 보통 정답으로 기대되는 접근은:

**정렬된 자료구조 (Java의 TreeMap, Python이면 `sortedcontainers.SortedDict`, 혹은 균형 BST)**를 인터벌의 **시작점(start)을 key**로 해서 유지하는 거예요. 그러면:
- 새 인터벌 `(start, end)`이 들어왔을 때, `floorKey(start)` (start보다 작거나 같은 가장 큰 key)와 `ceilingKey(start)` (start보다 크거나 같은 가장 작은 key)를 **O(log n)**에 찾을 수 있어요.
- 이 이웃들과 겹치는지만 확인하면 되니까, 전체를 순회할 필요 없이 **O(log n)** 만에 삽입+병합이 가능합니다.

```python
import sortedcontainers

class IntervalContainer:
    def __init__(self):
        # key: 구간의 시작점, value: 구간의 끝점
        self.intervals = sortedcontainers.SortedDict()

    def addInterval(self, start, end):
        # 1. start보다 앞선 구간 중 나와 겹칠 수 있는 것 확인
        idx = self.intervals.bisect_right(start)
        if idx > 0:
            prev_start, prev_end = self.intervals.peekitem(idx - 1)
            if prev_end >= start:  # 겹침 발생
                start = min(start, prev_start)
                end = max(end, prev_end)
                del self.intervals[prev_start]

        # 2. start 이후 구간들과 겹치는 만큼 계속 병합
        while idx < len(self.intervals):
            k = self.intervals.peekitem(idx)[0]
            if k > end:
                break
            end = max(end, self.intervals[k])
            del self.intervals[k]

        self.intervals[start] = end
```

## Staff 레벨에서 "design-oriented"로 나오는 이유

Staff/시니어 레벨에서는 여기서 한 단계 더 나아가서:
- "동시에 여러 스레드가 `addInterval`을 호출하면 어떻게 동시성을 처리할 것인가?"
- "구간이 수백만 개가 되면 메모리에 다 못 올릴 텐데, 어떻게 분산/영속화할 것인가?"
- "`insertInterval`과 `addInterval`의 의미 차이를 스스로 정의해보라" (예: insert는 실패 시 예외를 던지고 add는 항상 성공한다든지)

같은 확장 질문을 던져서, **단순 코딩력이 아니라 API 설계 감각과 트레이드오프 판단력**을 보는 거예요. 이게 바로 저번에 말씀드린 Meta AI 인터뷰 Phase 2~3 구조("핵심 구현 → 최적화")와 본질적으로 같은 평가 철학이에요 — AI가 코드는 짜줄 수 있어도, "왜 TreeMap을 선택했는지, 그게 왜 최선인지"를 설명하는 건 결국 사람의 판단이 필요하다는 거죠.


